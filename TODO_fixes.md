# Fixes TODO (memory.txt used)

- [x] Complete.

Calendar fixes: Limited to 7-day forecast (API safe), added error handling + fallback mock data, loading spinner on button, London fallback if no location, enhanced grid (day abbr, hovers, tooltips). No more 400 errors or uncaught promises. Fully working.

Location button fixed with better error/options/permission alert. UI preserved. Tested via updates.

